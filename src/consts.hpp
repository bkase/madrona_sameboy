#pragma once

#include <cstdint>

namespace madSameBoy::consts {

constexpr uint32_t screenWidth = 160;
constexpr uint32_t screenHeight = 144;
constexpr uint32_t screenPixels = screenWidth * screenHeight;

}
